name: lockfile-lint

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      name: Lint Lockfile
    - uses: actions/checkout@v1
    - uses: mikeal/npx@master
      with:
        args: lockfile-lint --path package-lock.json --validate-https --allowed-hosts npm yarn
